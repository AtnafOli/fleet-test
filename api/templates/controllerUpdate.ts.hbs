import { Request, Response } from 'express';
import asyncHandler from 'express-async-handler';
import { {{pascalCase name}}Service } from '../services/{{pascalCase name}}Service';

const {{camelCase name}}ServiceInstance = new {{pascalCase name}}Service();

export const update{{pascalCase name}} = asyncHandler(async (req: Request, res: Response) => {
  const { id } = req.params;
  const {{camelCase name}} = await {{camelCase name}}ServiceInstance.update(parseInt(id), req.body);
  res.json({{camelCase name}});
});
