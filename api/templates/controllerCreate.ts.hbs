import { Request, Response } from 'express';
import asyncHandler from 'express-async-handler';
import { {{pascalCase name}}Service } from '../services/{{pascalCase name}}Service';

const {{camelCase name}}ServiceInstance = new {{pascalCase name}}Service();

export const create{{pascalCase name}} = asyncHandler(async (req: Request, res: Response) => {
  const {{camelCase name}} = await {{camelCase name}}ServiceInstance.create(req.body);
  res.status(201).json({{camelCase name}});
});
